import React from "react";
// material-ui
import { Grid } from "@mui/material";

// Este componente es similar al de cálculos civiles por ahora, como se especificó
function DocumentsLayout() {
	return (
		<Grid container spacing={3}>
			<Grid item xs={12}>
				{/* Por ahora es un componente vacío, similar a Civil */}
			</Grid>
		</Grid>
	);
}

export default DocumentsLayout;
